# GoodReader

GoodReader 将本地静态阅读内容组织为统一书架，让读者从一个入口发现并阅读不同书籍。

## Language

**书架**：
首页中当前可阅读且不存在导入冲突的书籍集合。
_避免使用_：书库目录、books 目录、全部候选包

**静态书籍包**：
一本可独立导入和阅读的不可变内容单元，包含书架展示信息与完整的静态阅读内容。
_避免使用_：书籍工程、书籍服务、网站项目

**阅读状态**：
读者阅读一本书时产生并由 GoodReader 统一持有的外置信息，包括阅读进度及阅读标注；阅读状态不改变书籍内容。
_避免使用_：书籍数据、页面状态、阅读器缓存

**阅读标注**：
读者附加到书籍内容上的高亮、笔记或书签，由 GoodReader 持有且不改变书籍内容。
_避免使用_：标签、正文修改、书籍批注

**高亮**：
读者为一段选中文字添加的视觉强调。

**笔记**：
读者围绕一段书籍内容撰写的个人文字。

**书签**：
读者为了稍后返回而保存的一段选中文字，选中文字是书签的锚点。

**阅读进度**：
读者最后停留在一本书中的阅读位置；返回前文并在那里结束阅读时，进度可以回退。
_避免使用_：最远阅读位置、已读比例、完成度

**书籍接入契约**：
静态书籍包成为 GoodReader 书籍时必须满足的约定，用于稳定识别书籍、章节及可标注的正文位置。
_避免使用_：导入规则、页面猜测、网站兼容层

**书籍来源**：
用户交给 GoodReader 制作书籍的原始输入，可以是 PDF 文件、本地 HTML 目录或在线链接；书籍来源本身不直接进入书架。
_避免使用_：待导入书籍、原始书籍包、阅读文件

**在线书籍来源**：
以一个公开起始链接为入口，由 GoodReader 发现同源、同路径范围内的候选章节，并经用户确认章节清单后形成的书籍来源；也可以只包含起始页面。
_避免使用_：整站镜像、无限爬取、自动导入整个网站

**书籍转换**：
GoodReader 在用户的 Mac 上将书籍来源生成为遵守书籍接入契约、只执行 GoodReader 脚本的静态书籍包；书籍来源保持不变。
_避免使用_：直接导入、云端制书、脚本放行、自动信任

**确定性转换器**：
GoodReader 持有的本地转换组件，负责来源提取、静态化、稳定标识生成和可重复校验；它不判断翻译语义，也不以 Agent 的完成声明替代验证。
_避免使用_：Agent、翻译器、书籍生成任务

**来源快照**：
书籍生成任务开始时在任务工作区保存的不可变来源副本，用于保证转换、翻译和恢复基于同一内容；它不是正式书籍的一部分。
_避免使用_：原始书籍、书籍包、永久备份

**书籍生成任务**：
书籍来源进入书架前所经历的暂存过程，覆盖转换、可选翻译和最终校验；只有全部选定步骤成功后，结果才作为完整静态书籍包一次性进入书架。
_避免使用_：导入中的书、半成品书、后台下载

**主叙述语言**：
一本书承担主要说明和表达作用的自然语言；代码、命令、专有名词和少量引用不用于改变这一判断。
_避免使用_：字符占比、页面语言、出现过的语言

**目标语言**：
书籍翻译任务要生成的主正文语言；首版固定为简体中文 `zh-CN`，但任务模型保留语言字段以便未来扩展。
_避免使用_：界面语言、来源语言、对照文本语言

**语言判断**：
GoodReader 对书籍来源主叙述语言作出的“中文为主”“非中文为主”或“混合或无法确定”三态结果，用户可以在生成前覆盖该结果。
_避免使用_：自动翻译开关、语言猜测、中文字符检测

**PDF 预检**：
GoodReader 在生成书籍前对 PDF 页面的文本层和图像内容进行检查，将来源识别为数字 PDF、扫描 PDF 或混合 PDF，并指出需要 OCR 的页面。
_避免使用_：PDF 导入、OCR、格式转换

**Agent 运行时**：
GoodReader 调用的、本机已安装且已独立完成认证和模型配置的 Agent CLI。Agent 运行时持有自身的服务凭据与模型选择，GoodReader 不保存或复制这些配置。
_避免使用_：模型供应商、GoodReader 内置模型、API 配置

**自定义 Agent 运行时**：
用户自行添加到 GoodReader 的其他本机 Agent CLI，通过通用接入契约参与书籍 AI 工作，并继续独立管理自身认证和模型配置。
_避免使用_：自定义模型、API 供应商、任意后台脚本

**Agent 适配器**：
GoodReader 与一种 Agent CLI 之间的协议转换层，负责发现、启动、输入输出、能力声明、会话恢复和终止等运行时差异，不负责模型选择或任务业务逻辑。
_避免使用_：模型驱动、任务执行器、CLI 配置文件

**Agent 任务**：
GoodReader 为书籍摘要、基于书籍的问答或书籍制作创建的一次受控 AI 工作，具有明确的输入、目标和结果；同一任务可以由不同 Agent 运行时接续执行。
_避免使用_：聊天消息、后台脚本、任意命令

**任务上下文包**：
GoodReader 从书籍 AI 工作区为一次 Agent 执行整理的可移植协作状态，包括相关任务历史、用户指令、书籍引用、既有结论和产物；用于让不同 Agent 运行时共享知识并接续工作。
_避免使用_：CLI 会话、聊天记录转储、模型上下文

**书籍 AI 工作区**：
一本书对应的本地 AI 协作空间，统一保存围绕该书产生的摘要、问答、书籍制作过程、任务历史和产物；参与该书工作的不同 Agent 运行时共享这些信息。
_避免使用_：单 Agent 会话、隔离任务集合、云端知识库

**任务工作区**：
GoodReader 为一次 Agent 任务准备的隔离目录，包含任务所需的只读输入和独立的可写候选产物；Agent 运行时不通过它直接访问 GoodReader 数据库或正式书架。
_避免使用_：书库目录、应用数据目录、Agent 沙箱

**AI 书籍视图**：
GoodReader 从静态书籍包派生的本地只读文本表示，保留章节、正文块稳定标识和图片引用，供 Agent 检索、引用和理解书籍；它不是新的书籍副本，也不改变正文。
_避免使用_：向量库、翻译副本、Agent 专用书籍

**本地任务执行器**：
GoodReader 在本机运行的轻量后台组件，负责持久化调度 Agent 任务、启动和终止 Agent CLI、保存检查点并向界面报告进度；它不持有模型凭据或模型配置。
_避免使用_：模型服务、Agent 运行时、云端任务队列

**Agent 执行**：
Agent 任务中由一个明确 Agent 运行时完成的一次执行过程；同一任务同一时间最多有一个 Agent 执行可以写入候选产物。
_避免使用_：Agent 任务、并发回答、CLI 会话

**任务检查点**：
Agent 任务中已经完成并通过基本校验、可以交给原运行时或其他运行时继续工作的稳定状态。
_避免使用_：流式输出、生成草稿、CLI 自动保存

**任务能力策略**：
GoodReader 在 Agent 任务开始前声明的本地访问和操作范围，例如读取指定书籍、检索正文、写入候选产物或抓取指定在线来源；它不包含模型、账号或 API 配置。
_避免使用_：CLI 权限弹窗、模型权限、完全信任

**Agent 产物**：
Agent 在书籍 AI 工作区中生成并可被后续任务复用的摘要、提纲、笔记建议、章节或候选书籍；产物以版本保存，并记录生成运行时和引用来源。
_避免使用_：聊天回复、正式书籍、覆盖文件

**书籍清单**：
静态书籍包内名为 `book.json` 的权威描述文件，声明书籍身份、书架展示信息和阅读入口。
_避免使用_：书籍配置、目录元数据、HTML 元信息

**展示书名**：
一本书在书架和阅读界面中使用的主标题；翻译书默认使用可编辑的中文译名。
_避免使用_：源文件名、原始书名、目录名

**原始书名**：
翻译书籍在书籍来源中使用的标题，作为可搜索和可展示的次要元数据保留。
_避免使用_：副标题、英文书名、展示书名

**章节**：
书籍清单中按标准阅读顺序声明的阅读单元，拥有在书籍生命周期内稳定的章节标识。
_避免使用_：页面、HTML 文件、路由

**正文块**：
章节中可被定位和标注的最小内容单元，拥有全书唯一且不可复用的稳定标识。
_避免使用_：段落序号、DOM 节点、文本片段

**正文块账本**：
书籍生成任务中记录每个来源正文块及其译文、保留状态和校验结果的完整性清单，用于证明没有正文被遗漏或无来源新增。
_避免使用_：翻译进度、对照文本、正文索引

**主正文**：
用于标准阅读顺序、进度计算和阅读标注的书籍内容。
_避免使用_：对照文本、导航内容、页面装饰

**语义图片**：
承担阅读信息的图表、示意图、截图、照片、地图或插图，无论其在来源中是单一图片还是由矢量、文字和多个图层组合而成。
_避免使用_：所有图像对象、页面装饰、背景资源

**对照文本**：
与主正文块对应的可选只读内容，用于展示另一语言或版本；翻译书籍选择“支持显示原文”时，原文以正文块级对照文本保存。映射可以只覆盖部分正文块；对照文本不参与进度计算，也不能添加阅读标注。
_避免使用_：第二正文、可标注原文、翻译脚本

**书籍标识**：
静态书籍包创建时生成并永久保留的 UUID，用于区分书籍并关联其阅读状态；书名不要求唯一。
_避免使用_：书名、目录名、路径、ISBN

**导入冲突**：
扫描到的多个静态书籍包声明了同一书籍标识的状态；冲突解除前，这些书籍包均不可阅读。
_避免使用_：重复书籍、同名书籍、覆盖更新

**移除书籍**：
静态书籍包不再出现在书架中，但其阅读状态继续保留，以便相同书籍标识再次出现时恢复。
_避免使用_：删除书籍、清空书籍

**永久忘记**：
删除一本书全部用户数据的显式操作，包括阅读状态、阅读标注和书籍 AI 工作区；不负责删除静态书籍包或原始来源文件。
_避免使用_：移除书籍、隐藏书籍

**清除 AI 工作区**：
删除一本书的任务历史、Agent 产物和执行日志，但保留静态书籍包、阅读进度与阅读标注的显式操作。
_避免使用_：永久忘记、移除书籍、清空笔记
