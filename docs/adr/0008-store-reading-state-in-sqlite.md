# 阅读状态由本地服务持久化到 SQLite

浏览器只承载阅读界面，GoodReader 本地服务将全部阅读状态统一保存到单个 SQLite 数据库，不使用书籍页面的 localStorage 作为权威来源，也不把状态写回静态书籍包。相比直接依赖浏览器存储，这增加了一个本地数据层，却让进度与标注能够独立于浏览器缓存和配置长期保存，并为一致读写与备份提供可靠边界；第一版不引入账号、云同步或多用户。
