# 使用带会话认证的临时回环 HTTP 服务

正式应用每次启动时只在 `127.0.0.1` 上申请随机空闲端口，并生成仅本次运行有效的会话凭证；WebView 完成一次性换取会话后，所有状态写入同时校验会话、来源和内容类型，不开放 CORS，退出应用即停止服务并使会话失效。相比固定端口和无认证 localhost 接口，这增加了少量启动握手，却避免端口冲突，并降低其他网页或本地进程猜测和调用 GoodReader 数据接口的风险。
