# 可恢复地删除书库副本

书架菜单提供“删除书籍副本”，后端只允许把 GoodReader `Books/` 下经过真实路径解析的直接子目录移到 macOS 废纸篓。该操作不触碰最初选择的导入目录，也不删除 SQLite 中的阅读进度、高亮、笔记和书签；用户可以从废纸篓取回书籍包，或通过同一 UUID 的包恢复既有阅读状态。

删除书籍文件与“永久忘记阅读状态”是两个独立动作，界面分别说明影响并要求确认。这样既避免把文件移除误解为销毁个人阅读数据，也让误删书籍仍可通过系统废纸篓恢复。
