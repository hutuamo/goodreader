# book.json 是书籍元数据的唯一来源

每个完成导入的静态书籍包必须在根目录提供全小写的 `book.json`，运行时不从目录名、文件名或 HTML 内容推断书籍身份与展示信息。应用的 HTML 目录转换器可以在创建新包时推断初始标题、作者、封面、入口与章节，但必须分配新 UUID、写出完整清单并通过契约校验后才进入书架。显式清单使目录重命名不会切断已有阅读状态；手工放入且缺失或无效的清单仍会被报告。
