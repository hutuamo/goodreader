# V1 交付本机使用的 arm64 ad-hoc 签名版本

GoodReader V1 面向当前 Apple Silicon Mac，交付 arm64 `.app` 与可挂载 `.dmg`，采用 ad-hoc 签名并验证实际包内容与启动行为；不包含 Developer ID、公证、Intel 或 Universal Binary、自动更新及公开发布流程。这个范围满足个人本地工具的桌面封装目标，并将面向其他用户分发所需的身份、兼容与发布工程留待后续。
